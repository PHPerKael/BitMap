# BitMap
bigdata sort number

a pratise of bigdata sorting，use some common util or class，like File，FileOutputStream，RandomAccessFile，HashMap，BufferedOutputStream，ByteBuffer，MappedByteBuffer，FileInputStream.

as a newer of java，code mind be unreasonable，but I will be willing to see any good pull requests.
